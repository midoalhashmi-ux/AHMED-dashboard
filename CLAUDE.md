# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## اقرأ `TECHNICAL.md` قبل أي تعديل

هذا الملف يحتوي مرجعاً تقنياً كاملاً (بنية الملفات، الاتصال بـFirebase،
كل اللوحات، وجدول "سجل المشاكل والحلول" بأكثر من 12 حالة حقيقية موثَّقة).
**أضف صفاً جديداً فيه لأي إصلاح جوهري لاحق** بنفس التنسيق المختصر
الموجود، ولا تُعِد تشخيص مشكلة موثَّقة هناك من الصفر.

## ⚠️ فخّ النشر — لا تفترض أن أي تعديل ظاهر للمستخدمين فوراً

الموقع الفعلي يُنشر عبر **GitHub Pages من فرع `main` فقط**. التطوير يتم
على فرع منفصل — أي كوميت مدفوع لفرع التطوير **لا يظهر على الموقع
المباشر إطلاقاً** حتى يُدمَج فعلياً بـ`main` ويُدفَع. هذا سبّب التباساً
حقيقياً موثَّقاً بـ`TECHNICAL.md` (سجل #4). بعد أي دفعة تعديلات لهذا
المستودع تحديداً (خلاف sports_player/BinSheikh اللي تُبنى عبر Codemagic/
محلياً): `git checkout main && git merge <فرع التطوير> && git push origin main`.

## بنية بلا أي أداة بناء — HTML/JS/CSS خام

لا `package.json`، لا bundler، لا npm scripts. `index.html` يحمّل
`app.js`/`site-importer.js`/`ratings.js` مباشرة كـ`<script type="module">`،
و`app.js` يستورد Firebase v10.12.2 مباشرة من `gstatic.com` (بدون npm).
للمعاينة المحلية: أي خادم ثابت بسيط (`python3 -m http.server`) — فتح
`index.html` مباشرة كملف (`file://`) قد يفشل بقيود CORS لوحدات ES.

**⚠️ خطر حقيقي متكرر بـ`app.js` (~2250 سطر)**: module واحد بلا bundler —
أي خطأ غير ملتقط بنطاق top-level (خارج أي دالة، مثل `.addEventListener()`
على عنصر `querySelector` رجع `null`) **يوقف تنفيذ الملف بالكامل بصمت**.
عند إضافة أي مرجع لعنصر DOM جديد (`#id-جديد`)، تأكد أن الـ`id` موجود
فعلاً بـ`index.html` بنفس الإملاء بالضبط قبل أي `.addEventListener`/
تفاعل مباشر بنطاق top-level.

## البنية المعمارية — جزء من منظومة 3 مستودعات

يشترك هذا المستودع بنفس مشروع Firebase (`sports-stream-app-36a7a`،
`firebaseConfig` بأعلى `app.js`) ونفس Cloudflare Worker
(`binsheikh-api.binsheikh.workers.dev`، كوده بمستودع BinSheikh
`cloudflare-worker/`) مع:

- **BinSheikh** — تطبيق المحتوى الرئيسي (Flutter). يقرأ نفس مجموعات
  Firestore اللي هذي اللوحة تكتب فيها مباشرة (`categories`, `channels`,
  `settings/*`, `importSites`). أي تغيير باسم حقل/مجموعة هنا **يكسر
  BinSheikh مباشرة** بدون أي تحذير وقت التطوير — راجع `BinSheikh/
  CLAUDE.md` وقواعده قبل أي تعديل بنيوي على هذي المجموعات.
- **sports_player** — تطبيق تشغيل الفيديو المنفصل؛ لا تكتب هذي اللوحة
  فيه مباشرة، لكن حقول مثل `sources[]`/تسمية "سيرفر 1/2" (سجل #7
  بـTECHNICAL.md) يقرأها مباشرة كأزرار سيرفرات بديلة بالمشغّل.
- **هذا المستودع (AHMED-dashboard)** — اللوحة نفسها، بالإضافة لكودَي
  الاستيراد/التقييمات اللي يستدعيان الووركر المشترك:
  - `site-importer.js` (~1100 سطر) → `POST /import/site` — استيراد
    كتالوج/مسلسل من موقع خارجي (تصنيف نوع محتوى، فصل حلقات، دمج مصادر
    متعددة لنفس الحلقة بدل تكرارها بالكامل — راجع سجلَّي #5/#8
    بـTECHNICAL.md ففيهما تاريخ حساس من الأخطاء المتكررة بهذا التصنيف).
  - `ratings.js` (~123 سطر) → `POST /ratings/search` (Jikan للأنمي،
    TMDB للأفلام/المسلسلات).

**تنسيق ضروري بين المستودعات**: منطق فرز/فلترة الاستيراد الفعلي
(`guessContentType` وما يشابهها) موجود **بالطرفين معاً** — نسخة
مبسّطة/عرض هنا، ومنطق الجلب/التحليل الفعلي بالووركر
(`cloudflare-worker/src/index.js`, دوال `site*`). أي إصلاح لمشكلة
تصنيف/تكرار يحتاج مراجعة الملفين معاً، لا هذا المستودع وحده.

## أمان بسيط، لا تعامله كحدّ حقيقي

`CATEGORY_DELETE_PIN` (ثابت نصي بـ`app.js`، مرئي لأي زائر عبر عرض مصدر
الصفحة) هو مجرد احتكاك إضافي ضد حذف عرضي (سجل #9 بـTECHNICAL.md) — **ليس
طبقة أمان حقيقية**. الحد الفعلي الوحيد هو قواعد Firestore (`request.auth
!= null` لأي كتابة/حذف حقيقي بمستودع BinSheikh) + تسجيل دخول Firebase
Auth. لا تفترض أن تغيير هذا الثابت يحمي أي شيء فعلياً.

## اللوحات الرئيسية (تفصيل كامل بـTECHNICAL.md §5-6)

الأقسام (`categories`, شجرة `parentId`) والقنوات/الحلقات (`channels`)
والإحصائيات (`stats` — أُعيد بناؤها بالكامل، تبويبات نوع محتوى + مدى
زمني + drill-down على `dailyViews`) والإعدادات العامة (إعلانات AdMob،
تحديث إجباري، أزرار الشاشة الرئيسية `settings/homeButtons`، شروط
الاستخدام).
