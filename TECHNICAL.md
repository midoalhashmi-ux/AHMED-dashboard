# التوثيق التقني — AHMED-dashboard (لوحة التحكم)

> هذا الملف مرجع تقني شامل لهذا المستودع، بحيث يقدر أي مبرمج أو نموذج ذكاء
> اصطناعي آخر يفهم بنية المشروع كاملة بدون الحاجة لقراءة كل الكود من الصفر.
> **كل تعديل جوهري أو إصلاح مشكلة لاحق يجب أن يُضاف كسطر جديد في قسم "سجل
> المشاكل والحلول" بالأسفل** — بصيغة: ظهرت المشكلة كذا، والسبب الجذري كذا،
> وتم حلها بكذا، رقم الكوميت كذا.

## 1. نظرة عامة على المنظومة الكاملة (3 مستودعات مترابطة)

| المستودع | الدور |
|---|---|
| **BinSheikh** | تطبيق المحتوى الرئيسي (Flutter/Android) — يعرض للمستخدم النهائي الأقسام/القنوات/الأفلام/المسلسلات/الأنمي/مباريات اليوم. يحتوي أيضاً كود الووركر المشترك وقواعد أمان Firestore الفعلية. |
| **sports_player** | تطبيق Flutter/Android منفصل، وظيفته الوحيدة تشغيل الفيديو الفعلي. |
| **AHMED-dashboard** (هذا المستودع) | لوحة تحكم ويب ثابتة يديرها فريق المحتوى — إضافة/تعديل الأقسام والقنوات والروابط، ومتابعة إحصائيات المشاهدة. |

- **مشروع Firebase المشترك**: `sports-stream-app-36a7a` (نفس Firestore
  المستخدَم من التطبيقين). راجع `firebaseConfig` أعلى `app.js`.
- **Cloudflare Worker المشترك**: `https://binsheikh-api.binsheikh.workers.dev`
  (كوده المصدري بمستودع BinSheikh، `cloudflare-worker/`) — يُستخدم من هذه
  اللوحة عبر `site-importer.js` (`/import/site`) و`ratings.js`
  (`/ratings/search`).

## 2. طريقة النشر — **مهمة جداً لأي تعديل مستقبلي**

الموقع يُنشر عبر **GitHub Pages تلقائياً من فرع `main`** فقط. **التطوير
الفعلي يتم على فرع منفصل** (حالياً `claude/greeting-a1f5q5`) — أي تعديل
على هذا الفرع **لن يظهر على الموقع المباشر إطلاقاً** حتى يُدمَج
(`merge`) في `main` ويُدفَع (`push`) إليه.

⚠️ هذا سبّب التباساً فعلياً بتاريخ 2026-09-11: ظهرت تعديلات الإحصائيات
بالكود (كوميتات مدفوعة للفرع) لكن لم تظهر على الموقع الفعلي لأن `main`
لم يكن محدّثاً. **الخطوة الصحيحة بعد أي دفعة تعديلات لهذا المستودع**:
`git checkout main && git merge <الفرع> && git push origin main`
(أو فتح Pull Request ودمجه، حسب ما يفضّله المستخدم).

## 3. البنية — بدون أي أداة بناء (HTML/JS/CSS خام يُحمَّل مباشرة بالمتصفح)

- **`index.html`** (~436 سطر) — الصفحة الوحيدة؛ تحتوي كل لوحات الإدارة
  (الأقسام، الإحصائيات، الإعلانات، المشغل، التطبيق) كـ`<section>` تُبدَّل
  ظاهرة/مخفية عبر JS (`class="hidden"`).
- **`app.js`** (~2050 سطر) — كل منطق اللوحة، `<script type="module">`
  يستورد Firebase v10.12.2 مباشرة من `gstatic.com` (بدون npm/bundler).
  ⚠️ **نقطة ضعف معروفة**: لأنه module واحد بدون bundler، أي خطأ غير
  ملتقط في نطاق top-level (خارج أي دالة — مثلاً `.addEventListener()`
  على عنصر DOM أعاد `querySelector` له `null`) **يوقف تنفيذ بقية الملف
  بالكامل بصمت**. عند إضافة أي `document.querySelector('#id-جديد')` تأكد
  دائماً أن الـ`id` موجود فعلاً بـ`index.html` بنفس الإملاء.
- **`styles.css`** (~595 سطر) — كل التنسيق، بدون أي CSS framework.
- **`site-importer.js`** (~634 سطر) — استيراد محتوى من مواقع خارجية عبر
  `POST /import/site` بالووركر.
- **`ratings.js`** (~123 سطر) — جلب تقييمات TMDB/MyAnimeList عبر
  `POST /ratings/search` بالووركر.

## 4. الاتصال بـ Firebase والمصادقة

`firebaseConfig` بأعلى `app.js` يشير لنفس مشروع `sports-stream-app-36a7a`
المستخدَم بـ BinSheikh وsports_player بالضبط (نفس Firestore، نفس البيانات
حرفياً). تسجيل الدخول عبر Firebase Auth، ثم `onAuthStateChanged` يشغّل
تحميل كل اللوحات (`loadCategories`, `loadChannels`, `loadStats`, ...).

## 5. اللوحات الرئيسية

1. **الأقسام الرئيسية** (`categories`) — شجرة أقسام (قنوات/أفلام/
   مسلسلات/أنمي)، دعم أقسام فرعية (`parentId`)، إضافة/تعديل/حذف فردي
   وجماعي (تحديد للحذف).
2. **القنوات** (`channels`) — لكل قسم، تحديد مصدر (رابط مباشر/API
   ديناميكي)، ترتيب.
3. **الإحصائيات** (`stats`) — أُعيد بناؤها بالكامل هذه الجلسة (تفاصيل
   بالقسم التالي).
4. **الإعلانات، المشغل، التطبيق** — إعدادات عامة (AdMob، رسائل التحديث
   الإجباري، شروط الاستخدام...).

## 6. صفحة الإحصائيات (أُعيد تصميمها بالكامل — 2026-09-11)

- **تبويبات نوع المحتوى**: الكل/قنوات/أفلام/مسلسلات/أنمي
  (`data-stats-type`).
- **تبويبات مدى زمني**: اليوم/آخر 7 أيام/آخر 30 يوم/كل الوقت/مدى مخصص
  (`data-stats-range`) + صندوق تاريخ من-إلى عند اختيار "مدى مخصص".
- **"كل الوقت"** يستخدم `viewCount` التراكمي مباشرة (أسرع، بدون استعلامات
  فرعية). أي مدى آخر يستعلم عن مجموعة فرعية `dailyViews` لكل مستند عنده
  `viewCount > 0`، عبر
  `where(documentId(), '>=', from), where(documentId(), '<=', to)`،
  بالتوازي (`Promise.all`) — راجع `rankByViews`/`sumDailyViews` بـ
  `app.js`.
- **قائمة الأقسام قابلة للنقر (drill-down)**: الضغط على قسم (مثلاً
  "مسلسل كذا") يعرض ترتيب حلقاته/قنواته هو فقط (عبر
  `statsCategoryTreeIds` التي تتتبّع الأقسام الفرعية بنفس منطق شجرة
  الحذف، لكن بنسخة بيانات محلية مستقلة).
- **قائمة "فردياً"** (كل حلقة/قناة بمفردها بغض النظر عن قسمها) منفصلة،
  تُفلتر أيضاً حسب نوع المحتوى المختار.
- ⚠️ **قيد معروف موضّح بالواجهة نفسها**: أي مدى زمني غير "كل الوقت" لا
  يعرض إلا مشاهدات بعد تفعيل `dailyViews` بمستودع BinSheikh (لا يوجد أي
  تعبئة رجعية/backfill لمشاهدات قديمة قبلها).
- يعتمد هذا القسم بالكامل على قواعد Firestore وكود الكتابة الموثَّقَين
  بمستودع BinSheikh — راجع `BinSheikh/TECHNICAL.md` قسم "تدفق عدّاد
  المشاهدات".

## سجل المشاكل والحلول

1. **حذف جماعي للأقسام يفشل** برسالة "تعذر الحذف. تحقق من قواعد Firestore
   وحاول مرة أخرى" رغم أن أي طلب Firestore لم يُرسَل أصلاً — **السبب
   الجذري**: `allCategoryIds` متغيّر من نوع `Set` وليس `Array`، وكائن
   `Set` ليس له `.map()` فيرمي `TypeError` يُمسكه `catch` العام برسالة
   مضلِّلة. **الحل**: تحويله لمصفوفة أولاً
   `[...allCategoryIds].map(...)`. — الكوميت `8d55999`.
2. **تصميم الإحصائيات القديم مسطّح** (بدون تصنيف حسب نوع المحتوى، بدون
   فلترة زمنية) — أُعيد بناؤه بالكامل (راجع القسم 6 أعلاه). يتطلب هذا
   تعديلات موازية بمستودع BinSheikh (قواعد Firestore + كتابة `dailyViews`
   من `content_service.dart`). — الكوميت `37b38ed`.
3. **خطأ "تعذر الاتصال بقاعدة الأقسام" ظهر للمستخدم** بعد نشر قواعد
   Firestore — تحقيق كامل أثبت أن السبب **ليس بالكود** (`loadCategories`
   وقاعدة `categories.read` لم يُمسّا بأي تعديل بهذه الجلسة)، بل كان
   تعطّل شبكة/اتصال عابر انحلّ بمجرد تحديث الصفحة. أُضيف مع ذلك
   `console.error()` حقيقي بدل ابتلاع الخطأ بصمت بثلاث دوال
   (`loadCategories`, `loadChannels`, `loadStats`) لتسهيل تشخيص أي تكرار
   مستقبلي فوراً من Console بالمتصفح (F12). — الكوميت `86b3536`.
4. **تعديلات (2+3 أعلاه) لم تظهر على الموقع المباشر رغم دفعها لـ
   GitHub** — لأنها كانت مدفوعة فقط لفرع `claude/greeting-a1f5q5`،
   وGitHub Pages ينشر من `main` فقط (راجع القسم 2 أعلاه). **الحل**: دُمج
   الفرع في `main` ودُفع (fast-forward نظيف، بدون تعارضات). — merge
   `86b3536` → `main`.
